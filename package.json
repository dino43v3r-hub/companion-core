{
  "scripts": {
    "build": "node --check app.js && node -e \"const fs = require('fs'); for (const file of fs.readdirSync('platform-guides').filter((name) => name.endsWith('.json'))) JSON.parse(fs.readFileSync('platform-guides/' + file, 'utf8'));\""
  }
}
